<h1>To Do List</h1>
<hr />
<h2>Tasks:</h2>
<a href="/tasks"><button type="button" name="buttonSortType" value="dueDate">Due date</button></a>
<a href="/tasks"><button type="button" name="buttonSortType" value="description">Description</button></a>
<a href="/tasks"><button type="button" name="buttonSortType" value="other">Other</button></a>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var task in Model)
    {
      <li><a href='/tasks/@task.GetId()'>Name : @task.GetDescription()</a>
      <p>DueDate : @task.GetDueDate()</p>
      <p>Completed : @task.GetCompleted()</p>
      </li>
    }
  </ul>
}
<h4><a href="/tasks/new">Add new task</a></h4>
<h4><a href="/">Return home</a></h4>
